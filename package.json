{
  "name": "palmastro-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "prisma generate --schema=api/prisma/schema.prisma && cd client && npm install && npm run build",
    "vercel-build": "prisma generate --schema=api/prisma/schema.prisma && cd client && npm install && npm run build",
    "postinstall": "prisma generate --schema=api/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "prisma": "^4.16.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "@aws-sdk/client-s3": "^3.1037.0",
    "@aws-sdk/lib-storage": "^3.1037.0",
    "nodemailer": "^6.9.1",
    "passport": "^0.6.0",
    "passport-google-oauth20": "^2.0.0",
    "express-session": "^1.17.3",
    "razorpay": "^2.8.6",
    "bcrypt": "^5.1.0"
  }
}
